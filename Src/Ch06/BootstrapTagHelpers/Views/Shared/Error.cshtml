@using Ch06.BootstrapTagHelpers.Models
@model Ch06.BootstrapTagHelpers.Models.ViewModelBase

<h1>Error <span>@(Model.StatusCode > 0 ? Model.StatusCode.ToString() : "")</span></h1>

<h4>@(Model.ErrorMessage ?? "Sorry, it's not you!")</h4>

